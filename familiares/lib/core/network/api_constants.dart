abstract class ApiConstants {
  static const int hospitalId = 1;
  static const int appVersion = 1;
  static const Duration cacheMaxAge = Duration(hours: 8);
  static const Duration requestTimeout = Duration(seconds: 3);
}